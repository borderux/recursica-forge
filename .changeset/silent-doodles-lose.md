---
"recursica-forge": patch
---

- **HoverCardPopover Preview Styling**:
  - Removed hardcoded paragraph styles (`pStyle`) from the `HoverCardPopoverPreview` component, allowing the component's design tokens and text properties to be applied and adjusted correctly.
- **Component Preview & Layout**:
  - Enhanced the subtitle in `ComponentDetailPage` to display the active interaction state (e.g. "Base") instead of repeating the component's name when no custom variants or active states are selected.

