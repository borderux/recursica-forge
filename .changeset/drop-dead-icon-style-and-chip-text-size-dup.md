---
"recursica-forge": patch
---

Removed `globals_icon_style` and chip's duplicate `properties_text-size` — both were unconsumed by Forge and every adapter; chip now uses `properties_text_font-size` alone.
