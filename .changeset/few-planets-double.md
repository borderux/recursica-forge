---
"recursica-forge": patch
---

Add max-height prop to Menu component with vertical overflow scroll (default 600px). Adapters clamp dropdown height to viewport when it would extend below the visible area. Toolbar "Width" group renamed to "Size" and includes a new max-height slider (200px–1000px).
