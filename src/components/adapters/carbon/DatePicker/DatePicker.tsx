/**
 * Carbon DatePicker Implementation
 * 
 * Carbon-specific DatePicker component that uses CSS variables for theming.
 * Uses the same custom calendar popover as the Mantine implementation.
 */

// Re-export the Mantine implementation since the DatePicker is fully custom
// (not using any library-specific date picker components)
export { default } from '../../mantine/DatePicker/DatePicker'
