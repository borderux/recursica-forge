import type { TreeProps } from '../../common/Tree'

export default function Tree(props: TreeProps) {
  return <div className={props.className} style={props.style}>Material Tree Not Implemented</div>
}
