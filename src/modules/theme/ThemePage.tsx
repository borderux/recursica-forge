/**
 * ThemePage
 *
 * Unified Theme page that contains Palettes, Type, Layers, and Dimensions.
 * Uses ThemeSidebar for navigation (rendered in shell).
 */
import { Outlet } from 'react-router-dom'

export default function ThemePage() {
  return <Outlet />
}
